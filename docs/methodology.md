# Methodology

The CPU backend is for development, correctness and compatibility testing. CPU measurements must not be presented as Q.ANT photonic-hardware performance.

Initial experiments should record task accuracy/loss, parameter count, operator composition, Q.ANT SDK compatibility, numerical deviation from an independent reference, fixed random seed, and backend/package versions. Later, real Q.ANT hardware can add latency, throughput and energy as separate objectives.
